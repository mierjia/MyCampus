name: Deploy Vue App to GitHub Pages

on:
  push:
    branches:
      - main  

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

  dependencies-install:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Admin dependencies
        run: 
          npm i ./admin

      - name: Server dependencies
        run: 
          npm i ./server
     
      - name: Web dependencies
        run: 
          npm i ./web

      - name: Build Vue app
        run: 
          cd web 
          npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: main   # 部署到 gh-pages 分支
          folder: dist       # 构建后的文件夹
        env:
          GITHUB_TOKEN: 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  # 自动使用 GitHub token 进行身份验证
